apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myingress
  labels:
    name: myingress
spec:
  rules:
  - host: cf.local
    http:
      paths:
      - pathType: Prefix
        path: '/'
        backend:
          service:
            name: public-api
            port:
              number: 80
  - host: cf.backend
    http:
      paths:
      - pathType: Prefix
        path: '/'
        backend:
          service:
            name: backend-api
            port:
              number: 80
